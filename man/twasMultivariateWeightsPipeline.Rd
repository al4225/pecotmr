% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twas_weights.R
\name{twasMultivariateWeightsPipeline}
\alias{twasMultivariateWeightsPipeline}
\title{TWAS Multivariate Weights Pipeline}
\usage{
twasMultivariateWeightsPipeline(
  X,
  Y,
  mnmFit,
  L = NULL,
  Lgreedy = 5,
  cvFolds = 5,
  samplePartition = NULL,
  dataDrivenPriorMatrices = NULL,
  dataDrivenPriorMatricesCv = NULL,
  canonicalPriorMatrices = FALSE,
  mvsusieMaxIter = 200,
  mrmashMaxIter = 5000,
  maxCvVariants = -1,
  cvThreads = 1,
  verbose = 1
)
}
\arguments{
\item{X}{A matrix of genotype data where rows represent samples and columns represent genetic variants.}

\item{Y}{A matrix of phenotype measurements, where rows represent samples and columns represent conditions.}

\item{mnmFit}{An object containing the fitted multivariate models (e.g., mvSuSiE and mr.mash fits).}

\item{L}{Maximum number of components in mvSuSiE. If NULL, the number of
components in the fitted mvSuSiE object is used.}

\item{Lgreedy}{Initial greedy number of components in mvSuSiE. Defaults to 5.}

\item{cvFolds}{The number of folds to use for cross-validation. Defaults to 5. Set to 0 to skip cross-validation.}

\item{samplePartition}{Optional data frame with Sample and Fold columns for cross-validation. If NULL, a random partition is generated.}

\item{dataDrivenPriorMatrices}{A list of data-driven covariance matrices for mr.mash weights. Defaults to NULL.}

\item{dataDrivenPriorMatricesCv}{A list of data-driven covariance matrices for mr.mash weights in cross-validation. Defaults to NULL.}

\item{canonicalPriorMatrices}{If TRUE, computes canonical covariance matrices for mr.mash. Defaults to FALSE.}

\item{mvsusieMaxIter}{The maximum number of iterations for mvSuSiE. Defaults to 200.}

\item{mrmashMaxIter}{The maximum number of iterations for mr.mash. Defaults to 5000.}

\item{maxCvVariants}{The maximum number of variants to be included in cross-validation. Defaults to -1 which means no limit.}

\item{cvThreads}{The number of threads to use for parallel computation in cross-validation. Defaults to 1.}

\item{verbose}{Integer controlling verbosity level: 0 = suppress all messages,
1 = show pecotmr messages but suppress external package messages (default),
2 = show all messages including those from external packages.}
}
\value{
A list containing results from the TWAS pipeline, including TWAS weights, predictions, and optionally cross-validation results.
}
\description{
This function performs weights computation for Transcriptome-Wide Association Study (TWAS)
in a multivariate setting. It incorporates steps such as fitting models using mvSuSiE and mr.mash,
calculating TWAS weights and predictions, and optionally performing cross-validation for TWAS weights.
}
\examples{
# Example usage (assuming appropriate objects for X, Y, and mnmFit are available):
twas_results <- twasMultivariateWeightsPipeline(X, Y, mnmFit)
}
