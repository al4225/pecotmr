% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/misc.R
\name{detectOutliersMahalanobis}
\alias{detectOutliersMahalanobis}
\title{Detect Outliers via Mahalanobis Distance}
\usage{
detectOutliersMahalanobis(x, prob = 0.99, pvalThreshold = 0.05)
}
\arguments{
\item{x}{Numeric matrix (samples x features). Rownames are used as
sample IDs in the output.}

\item{prob}{Numeric in (0, 1); quantile threshold for the Mahalanobis
distance cutoff (default 0.99).}

\item{pvalThreshold}{P-value threshold for outlier classification
(default 0.05). A sample is flagged only if its distance exceeds
the quantile cutoff \emph{and} its p-value is below this threshold.}
}
\value{
A data.frame with columns:
  \describe{
    \item{sampleId}{Row names from \code{x}, or row indices if unnamed.}
    \item{mahal}{Mahalanobis distance.}
    \item{pvalue}{Chi-squared p-value (df = number of features).}
    \item{isOutlier}{Logical; TRUE if distance > quantile cutoff and
      p-value < \code{pvalThreshold}.}
  }
}
\description{
Identifies outlier samples in a numeric matrix (e.g., PCA scores) using
Mahalanobis distance with chi-squared-based p-values. Useful for QC
in genotype PCA or expression PCA workflows.
}
