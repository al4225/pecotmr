% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colocboost_pipeline.R
\name{qc_regional_data}
\alias{qc_regional_data}
\title{Initial QC for the region data loaded from \code{load_regional_data}}
\usage{
qc_regional_data(
  region_data,
  maf_cutoff = 5e-04,
  pip_cutoff_to_skip_ind = 0,
  keep_indel = TRUE,
  pip_cutoff_to_skip_sumstat = 0,
  qc_method = NULL,
  impute = FALSE,
  impute_opts = list(rcond = 0.01, R2_threshold = 0.6, minimum_ld = 5, lamb = 0.01)
)
}
\arguments{
\item{region_data}{A region data loaded from \code{load_regional_data}.}

\item{maf_cutoff}{A scalar to remove variants with maf < maf_cutoff.}

\item{pip_cutoff_to_skip_ind}{A vector of cutoff values for skipping individual contexts.}

\item{keep_indel}{Logical; if \code{FALSE}, remove indel variants during
summary-statistic allele harmonization.}

\item{pip_cutoff_to_skip_sumstat}{A vector of cutoff values for skipping summary-stat studies.}

\item{qc_method}{Quality control method to use. Options are "none",
"slalom", or "dentist". \code{NULL} is treated as \code{"none"} for
basic-only summary-stat preprocessing.}

\item{impute}{Logical; if TRUE, performs imputation when required metadata are available.}

\item{impute_opts}{A list of imputation options.}
}
\value{
A list containing post-QC \code{individual_data} and \code{sumstat_data}.
}
\description{
This compatibility wrapper converts loaded regional data to reusable individual
and RSS inputs, runs the shared QC helpers once, and returns the historical
post-QC structure consumed by \code{colocboost_pipeline()}.
}
