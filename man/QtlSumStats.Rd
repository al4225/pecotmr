% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qtlSumStats.R
\name{QtlSumStats}
\alias{QtlSumStats}
\title{Create a QtlSumStats Collection Object}
\usage{
QtlSumStats(
  study,
  context,
  trait,
  entry,
  genome,
  ldSketch,
  varY = NA_real_,
  qcInfo = list(),
  ...
)
}
\arguments{
\item{study}{Character vector of study identifiers (per tuple).}

\item{context}{Character vector of context labels (per tuple).}

\item{trait}{Character vector of trait identifiers (per tuple).}

\item{entry}{A list / \code{SimpleList} of \code{GRanges}, one per
tuple. Same length as \code{study}, \code{context}, and \code{trait}.}

\item{genome}{Single character string giving the genome build
(e.g., \code{"hg19"}, \code{"hg38"}). Uniform across the collection
because all entries share the same LD sketch.}

\item{ldSketch}{A \code{GenotypeHandle} carrying the LD reference.}

\item{varY}{Optional numeric vector of per-tuple phenotype variances
(\code{NA_real_} entries allowed).}

\item{...}{Additional per-tuple columns to attach to the collection.}
}
\value{
A \code{QtlSumStats} object.
}
\description{
Construct a \code{QtlSumStats} S4 DFrame-subclass
  collection from per-tuple vectors and a list of \code{GRanges}
  entries (one per tuple), plus a single LD sketch handle and a
  single genome build that apply to the whole collection. Each
  \code{GRanges} entry must carry per-variant statistics in its
  mcols (\code{SNP}, \code{A1}, \code{A2}, \code{Z}, \code{N}; plus
  optional \code{MAF}, \code{INFO}, \code{BETA}, \code{SE}, \code{P}).
}
